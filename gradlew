#!/usr/bin/env sh

# This is a placeholder. Run 'gradle wrapper --gradle-version 8.9' locally after cloning to generate the real gradlew.
# The CI will work because GitHub Actions checks out and the wrapper is bootstrapped in the workflow.
echo "Run gradle wrapper locally first or use Android Studio to generate gradlew"
exit 1
